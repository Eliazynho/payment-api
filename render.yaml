# render.yaml - Blueprint para deploy da API de Pagamentos no Render.com

# 1. Definição do Banco de Dados PostgreSQL
databases:
  - name: payment-api-db
    databaseName: payment_challenge
    user: elias_santos_dev
    plan: free

# 2. Definição do Serviço da Aplicação
services:
  - type: web # Serviço Principal da API
    name: payment-api
    runtime: node
    plan: free
    repo: https://github.com/Eliazynho/payment-api # Verifique se esta URL está correta
    branch: main
    buildCommand: npm install && npm run build
    # 👇 Comando que roda ANTES de cada deploy para aplicar as migrações
    preDeployCommand: npx prisma migrate deploy
    startCommand: npm run start:prod
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: payment-api-db
          property: connectionString
